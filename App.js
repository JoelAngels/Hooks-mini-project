import React from "react";
import TodoApp from "./TodoApp";
function App() {
  return <TodoApp />;
}

export default App;
//we are gonna have a TodoApp to manage the state
//TodoForm to code the form
//TodoList for the list of the todos
//TodoItem, items for the todos
